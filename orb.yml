version: 2.1
description: oneriver/get-secret

commands:
  hello:
    description: get-secret
    parameters:
      username:
        type: string
        description: your name
    steps:
      - run: 
          name: get-secret
          command: |
            USERNAME="$(echo << parameters.username >>)"
            echo "Hello ${USERNAME}"