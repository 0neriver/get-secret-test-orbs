version: 2.1

orbs:
  get-secret: oneriver/get-secret-manager@0.0.3

jobs:
  build:
    docker:
    - image: ubuntu-1604:202007-01
    steps:
    - run: "echo 'print username'"
    - get-secret/get:
        # username: "oneriver"

workflows:
  main:
    jobs:
    - build